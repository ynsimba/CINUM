{
  "name": "cinum-portail",
  "private": true,
  "description": "Portail institutionnel du civisme numérique — RDC",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev": "npx concurrently -n api,vite -c cyan,magenta \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "seed": "npm run seed --prefix server",
    "db:generate": "npm run db:generate --prefix server",
    "db:migrate": "npm run db:migrate --prefix server",
    "db:migrate:dev": "npm run db:migrate:dev --prefix server",
    "setup": "npm run db:migrate --prefix server && npm run seed --prefix server"
  }
}
