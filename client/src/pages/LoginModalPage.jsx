/**
 * Placeholder de route `/connexion` : le contenu réel est la {@link LoginModal} dans le Layout.
 */
export function LoginModalPage() {
  return (
    <div
      className="cinum-login-route-placeholder flex-grow-1"
      style={{ minHeight: 'min(40vh, 12rem)' }}
      aria-hidden="true"
    />
  )
}
